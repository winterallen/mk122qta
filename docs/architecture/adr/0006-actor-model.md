# ADR-0006: Actor 模型渐进式落地

## 状态

Accepted

## 背景

策略、风控、执行、元调度都需要隔离故障半径，但 P0 阶段直接引入完整 Actor 运行时会拖慢基建。

## 决策

P0 先用包边界、事件契约和应用进程边界模拟 Actor 职责；P3 多策略并行时再选择具体 Actor 运行时或自研 supervisor。

## 后果

早期开发保持轻量，后续需要在 P3 前补充进程监督、重启策略和事件幂等。
