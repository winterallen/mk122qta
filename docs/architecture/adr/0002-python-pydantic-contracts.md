# ADR-0002: Python 3.11 与 Pydantic 数据契约

## 状态

Accepted

## 背景

系统需要在数据接入、事件、回测、风控与观测之间传递强类型结构。

## 决策

P0 统一使用 Python 3.11+，进程内数据契约使用 Pydantic v2。跨进程二进制契约在后续阶段再引入 Protobuf。

## 后果

P0 可以优先获得运行时校验、清晰错误与 IDE 类型提示。Protobuf 的兼容性治理推迟到多进程事件规模扩大后处理。
