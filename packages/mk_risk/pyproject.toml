[project]
name = "mk-risk"
version = "0.1.0"
description = "Risk budgets and gate states for MK122 P1"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mk_risk"]
