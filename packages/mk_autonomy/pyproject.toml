[project]
name = "mk-autonomy"
version = "0.1.0"
description = "NAS, RL scheduler, and adaptive execution primitives for MK122 P7"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mk_autonomy"]
